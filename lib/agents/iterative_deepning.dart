import 'package:ai_project/agents/agent.dart';

class IterativeDeepning extends Agent {
  IterativeDeepning(int depth) : super(depth);
  @override
  Map calcBestMove(gamestate, plr) {
    throw UnimplementedError();
  }
}
